import { describe, expect, it } from 'vitest'; import { can } from './permissions'; describe('role permissions', () => { it('keeps viewers read-only', () => { expect(can('viewer', 'read')).toBe(true); expect(can('viewer', 'operate')).toBe(false); }); it('allows operators to work but not adjust', () => { expect(can('operator', 'reverse')).toBe(true); expect(can('operator', 'adjust')).toBe(false); }); it('allows admins to manage users and audit', () => { expect(can('admin', 'manage_users')).toBe(true); expect(can('admin', 'view_audit')).toBe(true); }); });
