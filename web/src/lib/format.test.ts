import { describe, expect, it } from 'vitest'; import { formatDate, formatQuantity } from './format'; describe('Italian formatting', () => { it('formats dates in Europe/Rome', () => expect(formatDate('2026-03-12T23:30:00Z')).toContain('13 mar 2026')); it('formats decimal quantities', () => expect(formatQuantity(1234.5, 'PZ')).toBe('1234,5 PZ')); it('formats missing values', () => expect(formatQuantity(null)).toBe('—')); });
