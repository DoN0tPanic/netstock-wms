import { useEffect } from 'react';
export const useHotkeys = (handlers: Record<string, () => void>) => useEffect(() => { const listener = (event: KeyboardEvent) => { const target = event.target as HTMLElement | null; const editing = target?.tagName === 'INPUT' || target?.tagName === 'TEXTAREA' || target?.tagName === 'SELECT'; if (editing) return; if (event.key === '/') event.preventDefault(); const handler = handlers[event.key]; if (handler) handler(); }; document.addEventListener('keydown', listener); return () => document.removeEventListener('keydown', listener); }, [handlers]);
