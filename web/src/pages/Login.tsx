import { useState } from 'react'; import { Navigate, useLocation } from 'react-router-dom'; import { Button, Input, PasswordInput } from '../components/ui'; import { useAuth } from '../hooks/useAuth';
export function Login() { const { session, login } = useAuth(); const location = useLocation(); const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); if (session) return <Navigate to="/" replace/>; return <main className="flex min-h-screen items-center justify-center bg-slate-900 p-4"><form className="w-full max-w-sm space-y-4 rounded-xl bg-white p-6 shadow-xl" onSubmit={(event) => { event.preventDefault(); setLoading(true); setError(''); void login({ username, password }).catch(() => setError(password !== password.trim() ? 'Credenziali non valide. Attenzione: la password inizia o finisce con uno spazio — sui telefoni la barra dei suggerimenti lo aggiunge da sola.' : 'Credenziali non valide o account temporaneamente bloccato.')).finally(() => setLoading(false)); }}><div><h1 className="text-2xl font-bold">NetStock</h1><p className="text-slate-600">Accedi al gestionale di magazzino</p></div><Input label="Nome utente" autoComplete="username" autoCapitalize="none" autoCorrect="off" spellCheck={false} required value={username} onChange={(event) => setUsername(event.target.value)}/><PasswordInput label="Password" autoComplete="current-password" required value={password} onChange={(event) => setPassword(event.target.value)}/>{error && <p role="alert" className="text-sm text-red-700">{error}</p>}<Button className="w-full" loading={loading} type="submit">Accedi</Button>{location.state && <p className="text-sm text-slate-600">Accedi per continuare.</p>}</form></main>; }
