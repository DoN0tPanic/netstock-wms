import type { ReactNode } from 'react'; export function Page({ title, description, actions, children }: { title: string; description?: string; actions?: ReactNode; children: ReactNode }) { return <div className="space-y-6"><header className="flex flex-wrap items-start justify-between gap-3"><div className="min-w-0"><h1 className="text-2xl font-bold">{title}</h1>{description && <p className="mt-1 text-slate-600">{description}</p>}</div>{actions && <div className="flex min-w-0 flex-wrap gap-2">{actions}</div>}</header>{children}</div>; }
export const Loading = () => <p role="status">Caricamento…</p>; export const ErrorMessage = () => <p role="alert" className="rounded-lg bg-red-50 p-4 text-red-800">Dati non disponibili. Verifica che il servizio sia raggiungibile e riprova.</p>;
